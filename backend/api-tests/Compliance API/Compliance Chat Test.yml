info:
  name: Compliance Chat Test
  type: http
  seq: 1

http:
  method: POST
  url: http://127.0.0.1:8000/api/chat
  body:
    type: json
    data: |-
      {
        "question": "What is the priority sector lending target for banks?"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
